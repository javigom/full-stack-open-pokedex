name: Say hello

on:
  push:
    branches:
      - master
jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo the message
        run: echo "Hello, world!"
      - name: Write the date
        run: date
      - name: List files
        run: ls -al            